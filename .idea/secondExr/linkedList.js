//elena-efimova

//Хранение элемента и указание на следующий
function Node(value) {
    this.value = number;
    this.next = null;
}

function add(value) {
    let element = new Node(value);
    let length = 0;

    let lastElement;
    let firstElement;

    //Пустой список
    if()

    
}