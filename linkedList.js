//elena-efimova

//Хранение элемента и указание на следующий
function Data(number) {
    this.number = number;
    this.next = null;
}

function add(value) {
    let element = new Data(value);


    
}